export const CDN_IMGURL="https://media-assets.swiggy.com/swiggy/image/upload/fl_lossy,f_auto,q_auto,w_660/"
//In above we have done the named export usually we use to do "export default" but in the case when we have more than one thing to export 
//it is not practical so we use "named export"while doing import just put the var name in "{}"
